void prime(void){
    int type;
    printf("prime: type of calculation >> ");
    scanf("%d", &type);
    switch (type)
    {
    case 1:
        printf("print prime: limit >> ");
        int lim;
        scanf("%d", &lim);
        for(int i = 0; i<lim;i++){
            
        }
        break;
    
    default:
        break;
    }
}